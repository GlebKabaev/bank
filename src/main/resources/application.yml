#Что нужно сделать:
#  - Указать настройки подключения к базе данных.
#  - Указать порт приложения.
#  - Настроить параметры безопасности и JWT.
#  - Включить Swagger UI.
# application.yml — главный конфигурационный файл Spring Boot.

spring:
  application:
    name: bank_rest
  datasource:
    url: jdbc:postgresql://localhost:5432/bank
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
token:
  signing:
    key: 53A73E5F1C4E0A2D3B5F2D784E6A1B423D6F247D1F6E5C3A596D635A75327855
app:
  user:
    exception-message:
      not-found-by:
        Id: "User with this ID was not found"
        Username: "User with this username was not found"
      already-exists-by:
        email: "User with this email already exists"
        username: "User with this username already exists"
  card:
    exception-message:
      already-exists-by:
        Id: "Card with this ID was not found"
        number: "Card with this number already exists"
      not-found-by:
        Id: "Card with this ID already exists"
        number: "Card with this number was not found"
      wrong-owner-exception: "The card owner doesn’t match the user’s card owner."

